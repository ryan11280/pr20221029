name: pr20221029
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.1.1
  cloud_firestore: ^4.0.3  #firebase
  firebase_storage: ^11.0.3 #最新版才能跑
  google_fonts: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  get: ^4.6.5

flutter:
  uses-material-design: true
  #這邊縮排必須對齊.............
  assets:
    - assets/images/
    - assets/icons/
    - assets/DB/papers/

